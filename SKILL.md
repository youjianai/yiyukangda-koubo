---
name: yiyukangda-koubo
description: |
  医誉康达抖音/视频号口播脚本写作与文档整理skill。
  输入：一条抖音医生（黄V）口播视频链接，或附带已提取的文案。
  自动完成五步：1.接收文案 2.解析病症/方剂/药材/穴位 3.核对专业信息 4.洗稿 5.整理成标准文档。
  触发词：「帮我洗稿」「处理这条视频」「做个脚本」「整理这条」「洗一下这个链接」以及直接把抖音链接发过来。
  不要用于：公众号长文写作（用 khazix-writer）、小红书图文、产品详情页文案。
allowed-tools: Read, Bash, Write, Edit, Glob
dependencies: python-docx
---

# 医誉康达口播脚本处理

## 用户身份

用户是一名医生IP编导，负责将抖音黄V医生的爆款口播视频洗稿后整理成标准化文档，供医誉康达品牌使用。目标受众以中老年人为主。

## 工作流程

收到链接（或链接+文案）后，按五步执行，每步完成后简单告知进度再进入下一步。

### 第一步：接收口播文案

- 用户发链接时通常已用轻抖等工具提取好文案一并贴过来，直接处理已有文案。
- 只发链接没给文案时，直接问：「文案发来」。

**输入质检**：收到文案后先快速判断，低质素材不硬洗。逐项填写，不下结论，让用户依据判断自行决定：

```
## 输入质检
- 钩子：[有/无] — [简述钩子类型，如痛点/好奇/反差/数字钩子]
- 干货：[有/无] — [简述实质内容，如方剂/穴位/辨证/具体方法]
- 赛道：[通过/不通过] — [是否中医/养生/健康科普？不相关写明原因]
```

三项都过关才进第二步；有一项明显缺失就如实反馈，让用户决定要不要继续。

**字数预检**：原文明显过长（≥350 字）时，洗稿需更积极压缩——引导语选短、解释段精简、症状罗列合并，确保成稿 ≤300 字。

### 第二步：解析文案内容

识别并列出病症、方剂（药材+克数）、单独药材、穴位。字段规范与识别细则见 [references/parse_check_spec.md](references/parse_check_spec.md)。

### 第三步：核对专业信息（内置知识）

**当前暂停联网**，基于内置中医/临床通识核对，输出鉴别诊断、禁忌人群、采摘炮制、穴位核对。把握不准的宁可不写或写保守；后三段整体标 `review_note`「以上为通用知识，请执业医师复核」，不附来源、绝不编造。完整规范（3a–3d、可靠性层级、恢复联网方式）见 [references/parse_check_spec.md](references/parse_check_spec.md)。

### 第四步：洗稿

洗稿是本 skill 核心——先拆功能段、搭叙事骨架，再顺骨架逐节点重讲、边写边缝。

**⚠️ 洗稿前必须先 `Read references/rewrite_playbook.md`，严格按其流程与 A–F 写作指令执行。** 该文件是洗稿规则的唯一事实源，本节不复述规则。

洗完做 4 项人工卡口（机器测不了的判断），其余交给机器校验：

```
## 洗稿卡口
- [ ] 叙事链每步问答接上（钩子→揭秘→痛点→桥+引导语→方子→收尾，无断层；尤其引导语两处接缝）
- [ ] 8 类灵魂钩子力度只增不减（未软化/泄气）
- [ ] 二创改动量 ≥30%（非逐句对译，无连续 10 字照抄）
- [ ] 引导语主题与文章匹配（通用型或已匹配，无伪因果绑定）
```

**定稿门禁**：定稿后、导出前必须跑机器校验，`error` 为 0 才能导出：

```bash
python scripts/count_chars.py '脚本正文'
python scripts/validate_output.py '脚本正文' --herbs "荷叶10克,陈皮6克" --syndromes "证型"
```

（`--herbs`/`--syndromes` 传第二步解析出的锁死项，做保全断言；不传则跳过该项。）

### 第五步：按标准格式输出文档

按固定格式整理成稿，再跑 `python scripts/build_docx.py <input.json路径>` 导出 Word。格式样例、后三段【】规则、input.json 段落映射见 [references/output_format.md](references/output_format.md)。

## 硬约束（所有步骤共用的编译时规则）

### 成稿字数上限
口播脚本正文 **总字数 ≤300 字**。洗完用 `python scripts/count_chars.py '正文'` 统计，超了压缩。

### 标点禁令
正文绝对禁止，出现即违规：
- **冒号「：」** → 用逗号代替或直接断开
- **破折号「——」** → 用逗号或句号代替
- **双引号「""」「""」** → 用「」代替，或不加引号

### 单位用汉字
所有计量单位字母缩写一律用汉字：g→克、mg→毫克、ml→毫升、cm→厘米、mm→毫米。药材克数统一输出汉字「克」（如「当归8克」），与原文用 g 还是克无关。阿拉伯数字本身保留（10克 不是 十克）。

### 绝对不能改
- 方剂的药材名称和克数，一个字不改
- 原文未给克数、或用「勺」「把」等份量词的食疗方，如实保留，不补不编克数
- 病症名称和证型，一个字不改
- 经第三步核对过的医学事实
- 标题（标题党不软化，见 playbook C 节）
- 引导语段不手改、不逐字洗，只从库整段替换/插入

## 角色边界

**模型负责**：解析文案、核对专业信息、按 playbook 洗稿、排版导出 docx、引导语入库（脱敏、去重、追加）。

**模型不负责**：医学准确性的最终判断、原始素材真伪核实（用户提供的文案默认真实）、成稿的发布决策（用户终审）。

**执业医师终审**：所有医学相关内容（方剂、禁忌、辨证、穴位）最终须经执业医师复核。

## 引导语入库（流程内动作，不打断主流程、不每次问）

洗稿取用引导语后，把本条**原文**的引导语段脱敏（删账号自称）后去重追加进 `references/guidance_library.json`：先归一化（去标点空白）再比对，完全相同/高度雷同的不重复入。新入条目建议带 `{"text":"...","universal":true/false}`——把点赞/关注与具体利益（省钱/长高等）挂钩的标 `false`，通用型（交医生朋友/有缘人）标 `true`。追加完必须验证 JSON 完整性：`python -c "import json; json.load(open('references/guidance_library.json', encoding='utf-8'))"` 不报错才算成功。

## 注意事项

- 每步完成后简单告知进度（如「文案已提取，正在解析……」）。
- 用户在链接外给了额外指令（如「这个不要改克数」「方子保留原文案」），以用户指令为准。
- docx 导出用固化脚本，不每次现写 python-docx 代码，也不逐条停下等用户确认（用户要改会主动提出）；跑完删掉临时 input.json，桌面与 skill 目录不留临时文件。详见 output_format.md。
